name: Teste Runner WSL2

on:
  push:
    branches:
      - main

jobs:
  test-runner:
    runs-on: self-hosted
    steps:
      - name: Verificar runner ativo
        run: echo "Runner no WSL2 está funcionando corretamente!"
